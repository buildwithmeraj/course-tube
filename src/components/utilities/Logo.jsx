import React from "react";
import Icon from "./Icon";

const Logo = () => {
  return (
    <div className="flex items-center gap-1 text-3xl font-bold">
      <Icon height={50} width={50} /> CourseTube
    </div>
  );
};

export default Logo;
